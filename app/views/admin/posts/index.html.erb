<div class="container">
  <div class="row">
    <div class="col-12">
      <h2 class="mb-3">ユーザー投稿一覧</h2>

      <table class="table">
        <thead class="thead-light">
          <tr>
            <th>ユーザー名</th>
            <th>ジャンル名</th>
            <th>タイトル</th>
            <th>記事</th>
          </tr>
        </thead>
        <tbody>
          <% @posts.each do |posts| %>
            <tr>
              <td>
                <%= link_to edit_admin_post_path(post) do %>
                  <%= post.created_at %>
                <% end %>
              </td>
              <td><%= post.user.name %></td>
              <td><%= post.genres %></td>
              <td><%= post.title %></td>
              <td><%= post.introduction %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
